# ==================================
# 1. PENGATURAN UMUM SERVER
# ==================================
PORT=5000
NODE_ENV=development

# URL frontend yang diizinkan CORS. 
# Ganti dengan domain frontend Anda saat deploy.
CLIENT_URL=http://localhost:3000

# ==================================
# 2. KONFIGURASI DATABASE (PRISMA)
# ==================================
# Contoh format PostgreSQL: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Contoh format MySQL: mysql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://user_db:password_kuat@localhost:5432/nama_aplikasi?schema=public"

# ==================================
# 3. PENGATURAN JWT (AUTHENTICATION)
# ==================================
# Kunci rahasia untuk menandatangani JWT. Harus panjang dan acak.
# JANGAN PERNAH MENGEKSPOS KUNCI INI KE PUBLIK.
JWT_SECRET_KEY="Kunci_Rahasia_Sangat_Panjang_Dan_Acak_Untuk_Token_JWT_Anda"

# Waktu kedaluwarsa token (misalnya 1h = 1 jam, 7d = 7 hari)
JWT_EXPIRES_IN="1d"

# ==================================
# 4. LAYANAN EKSTERNAL (Opsional)
# ==================================
# Contoh API Key untuk layanan email (misalnya SendGrid/Mailgun)
EMAIL_SERVICE_API_KEY="sg.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"